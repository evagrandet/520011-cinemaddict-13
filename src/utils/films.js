export const sortByDate = (filmA, filmB) => {
  console.info(filmA.releaseDate, filmB.releaseDate);
};


export const sortByRating = (filmA, filmB) => {
  console.info(filmA.releaseDate, filmB.releaseDate);
};
